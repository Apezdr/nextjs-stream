//export { auth as middleware } from "./lib/auth"

export default function proxy(req, res) {
  // This function intentionally does nothing.
}

/*export const config = {
  matcher: ["/list/:path*", "/api/authenticated/:path*", "/admin/:path*"],
};*/
